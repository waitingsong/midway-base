
export const cors = {
  enable: false,
  package: 'egg-cors',
}

export const kmore = {
  enable: true,
  package: 'egg-kmore',
}

export const jwt = {
  enable: true,
  package: '@waiting/egg-jwt',
}

export const fetch = {
  enable: true,
  package: '@waiting/egg-fetch',
}

// false 禁用全部安全检查用于临时调试
// export const security = false

